#!/usr/bin/bash

# read -p "Enter a terminal: " terminal

# echo "The terminal is ${terminal}."

# IFS=';' read -rp "Enter your values: " value1 value2

echo "value 1 is ${value1}"

echo "value 2 is ${value2}"

echo "${#}"
echo "${@}"
echo "${*}"
echo "${0}"